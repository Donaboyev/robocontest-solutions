#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    string a, b;
    cin >> a >> b;

    if (a.length() > b.length())
    {
        for (int i = 0; i < a.length(); i++)
        {
            /* code */
        }
        
    }
    else
    {
    }

    return 0;
}