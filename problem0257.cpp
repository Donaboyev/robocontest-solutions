#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    string n;
    cin >> n;
    if (((int)n.at(0)) == 1)
    {
    }
    for (int i = 0; i < n.length(); i++)
    {
        if ()
    }
    cout << ((a >= b && a >= c) ? a : (b >= c ? b : c));

    return 0;
}